name: Test

on:
  workflow_dispatch:

jobs:
  build:
    name: Test
    runs-on: ubuntu-24.04
    defaults:
      run:
        shell: bash

    steps:
    - name: Install LLVM
      uses: sbwml/actions@clang-22

    - name: clang version
      run: |
        clang --version
